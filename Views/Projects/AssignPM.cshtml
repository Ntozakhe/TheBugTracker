@model TheBugTracker.Models.ViewModels.AssignPMViewModel

@{
    ViewData["Title"] = "Assign PM";
}



<div>
    <h1>Project: @Model.Project.Name</h1>
    <div class="card shadow border-0 my-4">
        <div class="card-header bg-secondary bg-gradient text-light py-4">
            <div class="row">
                <div class="col-12 text-center">
                    <h3 class="text-white text-uppercase">Assign Project Manager</h3>

                </div>
            </div>
        </div>
        <div class="card-body">
            <form method="post" asp-action="AssignPM" asp-controller="Projects">
                <input type="hidden" asp-for="Project.Id" />

                <p class="fw-bold">@Model.Project.Name</p>

                <p>@Html.Raw(Model.Project.Description)</p>

                <div class="form-group">
                    <label class="control-label">Select Project Manager</label>
                    <select asp-for="PMID" class="form-control" asp-items="@Model.PMList">
                        <option></option>
                    </select>

                    <input type="submit" value="Assign" class="btn btn-primary mt-2" />
                </div>
            </form>
        </div>
        <a asp-action="UnassigendProjects">Back to List</a>
    </div>
</div>



@* <h1>Project: @Model.Project.Name</h1>
<h4>Assign Project Manager</h4>
<hr />
<div class="row d-flex justify-content-center">
    <div class="col-md-4">
        <form method="post" asp-action="AssignPM" asp-controller="Projects">
            <input type="hidden" asp-for="Project.Id" />

            <p class="fw-bold">@Model.Project.Name</p>
           
            <p>@Html.Raw(Model.Project.Description)</p>

            <div class="form-group">
                <label class="control-label">Select Project Manager</label>
                <select asp-for="PMID" class="form-control" asp-items="@Model.PMList">
                    <option></option>
                </select>

                <input type="submit" value="Assign" class="btn btn-primary mt-2" />
            </div>
        </form>
    </div>
</div>
<a asp-action="UnassigendProjects">Back to List</a> *@